@page
@model Restaurant.Pages.Ingredients.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ingredient[0].MenuId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ingredient[0].SKU)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ingredient[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ingredient[0].Unit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ingredient[0].Qty)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ingredient[0].Servings)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Ingredient) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MenuId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SKU)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Unit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Qty)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Servings)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IngredientId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IngredientId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IngredientId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
